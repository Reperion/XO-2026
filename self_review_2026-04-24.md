# XO Self-Review — 2026-04-24

_Generated by XO Self-Review cron at 21:00 CEST_

---

## 1. Sessions Analyzed

| Session | Time | Source | Model | Status |
|---------|------|--------|-------|--------|
| Workspace Installation | 19:40 | CLI | tencent/hy3-preview:free | ✅ Success |
| `!hermes update` + Debug | 20:36 | CLI | deepseek/deepseek-v4-flash | ⚠️ Partial — found issues |
| "hey deepseek" catch-up | 20:54 | CLI | deepseek/deepseek-v4-flash | ✅ Success (fixed crons) |
| "Can you see this?" | 20:51 | API | deepseek/deepseek-v4-flash | ✅ Success |
| Hourly Evolution Sprint | 21:00 | Cron | — | 🆕 Just started |

---

## 2. Errors & Failures Found

### 🔴 CRITICAL: Two Cron Jobs Failed at 20:00

Both the **Evening Report to Mike** (`35a7b78d9a58`) and **Hourly Evolution Sprint** (`df2ad140f738`) reported `last_status: "error"` at 20:00 today.

**Root cause:** OpenRouter credential pool exhaustion for model `tencent/hy3-preview:free`. The model was being rejected (401/400/404 errors in `errors.log`).

**Resolution:** XO diagnosed and fixed both crons at ~20:54 CEST — model changed from `tencent/hy3-preview:free` → `deepseek/deepseek-v4-flash`. The Evening Report cron's prompt and deliver target were also reviewed; still delivering to Telegram 8550634232. Next runs should succeed.

**⚠ Note:** Only 5/100 Evening Report repeats remain. When exhausted, the cron will stop permanently.

### 🔴 CRITICAL: DeepSeek 400 Reasoning Error

When Mike set `reasoning: medium` on deepseek/deepseek-v4-flash, Hermes returned HTTP 400:
```
The reasoning_content in the thinking mode must be passed back to the API.
```

**Cause:** Hermes client does not preserve and resend DeepSeek's `reasoning_content` from previous assistant turns. This is a known limitation — any use of reasoning modes (low/medium/high) will break.

**Impact:** If Mike tries reasoning mode again, the session will crash with 400 errors.

### 🟡 WARNING: Auxiliary LLM Provider Not Resolving

The compression auxiliary provider (`google/gemini-3-flash-preview` via `auto` mode) is resolving to `None`. The warning "No auxiliary LLM provider configured" appears on every session start.

**Root cause:** `get_text_auxiliary_client("compression")` returns `(None, None)` despite OpenRouter being properly configured. Likely a config version mismatch or resolution bug after the `hermes update`.

**Impact:** Context compression falls back to turn-dropping instead of summarization. This will degrade long-session quality.

### 🟡 WARNING: `HERMES_DASHBOARD_TOKEN` Not Set

After `hermes update` at 20:36, a deprecation warning appeared. The token is not set but is not blocking current operation.

### 🟢 RESOLVED: pytest Missing

SpaceX tracker tests couldn't run due to missing `pytest`. Fixed by `pip install pytest --user` in the 20:54 session.

---

## 3. Recurring Failure Patterns

| Pattern | First Seen | Occurrences | Status |
|---------|-----------|-------------|--------|
| OpenRouter credential pool exhaustion | Apr 8 | 5+ | 🔴 Active — fixed for crons but model routing is fragile |
| `&&` / `&amp;` shell escaping bug | Apr 9 | 4+ | 🔴 Active — HTML entity encoding in tool-call layer |
| Missing API keys (EXA, FIRECRAWL, BROWSERBASE) | Apr 8 | Every session | 🟡 Active — blocked since day 1 |
| Cloudflare CAPTCHA blocking browser | Apr 9 | 3+ | 🟡 Active — need Browserbase proxy |
| Model unavailable after switching | Apr 23 | 3+ | 🟢 Mitigated — moved to deepseek-v4-flash |
| DeepSeek reasoning mode crashes | Apr 24 (today) | 1 | 🔴 Active — new issue |

---

## 4. Skills That Need Improvement

| Skill | Issue | Action Needed |
|-------|-------|--------------|
| `xo-self-report` | Prompt references old model `tencent/hy3-preview:free`; needs update to `deepseek/deepseek-v4-flash` | Patch skill |
| `weather-query` | Never fully tested after bug fix | Test with edge cases |
| `hermes-agent` | Missing info about DeepSeek `reasoning_content` limitation | Add known issue |
| `reliable-scheduled-communication` | Evening Report cron prompt is 18 days old; uses old model reference | Update cron prompt |
| `xo-cron-manager` | Created but never tested with actual cron errors | Verify by running next cron cycle |

---

## 5. New Capabilities Needed

| Capability | Why | Priority |
|-----------|-----|----------|
| **DeepSeek reasoning fix** — patch Hermes client to preserve `reasoning_content` | Prevents crashes when Mike uses reasoning mode | 🔴 HIGH |
| **Credential pool monitoring** — detect and alert on exhausted API keys | Prevents silent cron failures | 🟡 MEDIUM |
| **Cron expiry alert** — Evening Report only has 5/100 repeats left | Cron will stop working soon; needs renewal | 🟡 MEDIUM |
| **Scheduled PENDING.md resolution reminders** — auto-nag Mike for API keys | 3 critical API keys blocked for 16+ days | 🟡 MEDIUM |
| **Model health check cron** — verify model availability before session | Prevents error cascades | 🟢 LOW |

---

## 6. Recommendations

### Immediate (this session)
1. **Patch `xo-self-report` skill** — update model reference
2. **Update 5 remaining Evening Report repeats** — extend before they expire
3. **Investigate and fix auxiliary compression provider** — document findings for future

### Before Tomorrow
4. **DeepSeek reasoning fix** — patch Hermes or add config workaround
5. **Extend Evening Report cron repeat count** from 5/100 to 100/100

### This Week
6. **Request API keys from Mike** (EXA, FIRECRAWL, BROWSERBASE) — 16+ days pending
7. **Test `xo-cron-manager` skill** against real cron failure scenarios
8. **Update AGENTS.md auto-gen proposal** — check message 664 status

---

## 7. Cron Health Summary

| Cron Name | Schedule | Model | Last Status | Health |
|-----------|----------|-------|-------------|--------|
| Evening Report to Mike | 20:00 daily | deepseek/deepseek-v4-flash | ❌ error (fixed) | ⚠️ 5/100 repeats left |
| Morning Research Sprint | 09:00 daily | minimax/m2.7 | never ran | ❓ Untested |
| Midday Skill Practice | 13:00 daily | minimax/m2.7 | ✅ ok (Apr 10) | 🟢 Good |
| Afternoon Capability Experiment | 15:00 daily | minimax/m2.7 | never ran | ❓ Untested |
| Weekly Memory & Docs Maintenance | Mon 10:00 | minimax/m2.7 | never ran | ❓ Untested |
| Hourly Evolution Sprint | Every hour | deepseek/deepseek-v4-flash | ❌ error (fixed) | 🟢 18/200 runs left |
| XO Self-Review | 21:00 daily | (inherits current) | ✅ ok (Apr 23) | 🟢 Forever |
| XO Skill Discovery | 10:00 daily | (inherits current) | never ran | 🟢 Forever |

---

_Generated by XO's autonomous self-review system. Analysis covers all sessions from April 24, 2026._
